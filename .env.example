# Copy to .env (gitignored). Used by the local grader route only.
ANTHROPIC_API_KEY=sk-ant-...
# Bearer-token setups may set this instead of ANTHROPIC_API_KEY:
# ANTHROPIC_AUTH_TOKEN=...
# Optional override; defaults to claude-opus-4-8
# GRADER_MODEL=claude-opus-4-8
